import Container from '@material-ui/core/Container';

const Navigation = () => {
   return (
        <Container
            className="App"
            maxWidth="sm">
            Navigation content
        </Container>
   );
}
export default Navigation;
